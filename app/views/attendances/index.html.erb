<div class="mt-20 ml-64 px-3 py-1">
  <div class="flex items-center justify-between">
    <p class="text-2xl font-bold">Attendance Records</p>
    <%= render 'mark_attendance_button' %>
  </div>

  <hr class="my-4">

  <%= render 'shared/records_table', headers: attendance_table_headers, rows: attendance_table_rows %>
</div>
